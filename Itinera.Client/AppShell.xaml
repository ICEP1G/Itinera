<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Itinera.Client.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Itinera.Client"
    Shell.FlyoutBehavior="Flyout"
    Title="Itinera.Client">

    <TabBar>
        <Tab Title="Home" Icon="nav_home_icon">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate local:HomePage}"
                Route="HomePage" />
        </Tab>
        <Tab Title="Profil" Icon="nav_itineros_icon">
            <ShellContent
                Title="Profil"
                ContentTemplate="{DataTemplate local:ProfilPage}"
                Route="ProfilPage" />
        </Tab>
    </TabBar>

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="Tests"
        ContentTemplate="{DataTemplate local:TestsPage}"
        Route="TestsPage" />

    <ShellContent
        Title="TestsReviews"
        ContentTemplate="{DataTemplate local:TestsReviewsPage}"
        Route="TestsReviewsPage" />

</Shell>
